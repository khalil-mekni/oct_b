type User {
    id: ID!
    name: String!
    email: String!
    role: String!
}

extend type Query {

    users: [User!]!
        @all
        @auth

    user(id: ID!): User
        @find
        @auth
}