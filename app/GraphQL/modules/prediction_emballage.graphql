type PredictionEmballage {
  periode: String!
  quantite_predite: Float!
  unite: String!
}

extend type Query {
  predictionEmballage(
    emballage_id: ID!
    entrepot_id: ID
    granularity: String
    periods: Int
    start_date: String
  ): [PredictionEmballage!]!
    @field(resolver: "App\\GraphQL\\Queries\\PredictionEmballageQuery")
}